enum Main {
    typealias View = MainViewProtocol & BaseView
    typealias ViewModel = MainViewModelProtocol & BaseViewModel
}
enum VideoDetails {
    typealias View = VideoDetailsViewProtocol & BaseView
    typealias ViewModel = VideoDetailsViewModelProtocol & BaseViewModel
}
